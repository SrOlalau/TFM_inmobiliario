Procesos de única ejecución:
1.Obtener los datos del POI ( Ok en el servidor ) | data/POI/POI_OSM_downloader.py ( Ok en main )
2.Tuning de datos de criminalidad ( Pendiente ) | data/criminalidad/indice__criminalidad.py (Ok en main )

Proceso de ejecución contínua:
1. Scrapeo de los datos ( Ok en servidor ) | En la carpeta web_scraping_scripts
2. Limpieza y juntar de los datos ( Ok en servidor ) | datamunging/datamunging.py (Ok en main )
3. Añadir geo localizaciones a los datos (Ok en servidor) | data/geolocations/add_geoloc.py 
4. Calcular las distancias de POI (Ok en main) | data/geolocations/calculate_POI_distance.py
5. Datatuning (PENDIENTE en servidor) | datatuning/data_tuning.py ( Ok en main )
6. Añadir indices de criminalidad ( extra ) para obtener el dataset final que usaremos para streamline | En (PENDIENTE en main )

8.Ejecutar el machine learning ( Pendiente ) | machinelearning/machinelearning.py ( Ok en main )

